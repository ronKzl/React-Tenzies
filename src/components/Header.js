import React from "react"

export default function Header(){
    return (<header className="header">
        <h2>Tenzies</h2>
        <h3 className='gameRules'>Roll until all dice are the same. Click each die to freeze it in between rolls.</h3>
    </header>)
}